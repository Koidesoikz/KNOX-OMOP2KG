def Main():
    #Read CSV's and save them in a tuple (Class, ID)
        #Also make tuple representing relations (SubjectID, Predicate, ObjectID)
    #Create header to RDF
        #Create link to local ontology
    #Write classes to RDF file
    #Write relations to RDF file
    return

if __name__ == "__main__":
    Main()